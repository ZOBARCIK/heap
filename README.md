# heap
Heap in C++

Im trying to work tidier thats why there are so many files

currently this repo can allocate and deallocate memory, manage them by max heap

todo: coalescing neighbours to merge previously freed blocks, alignemnt handling(rounding sizes)
